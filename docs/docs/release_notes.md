# Release notes

0.0.2 - Added project-mode metrics workflow

- Added `sources/`, `metrics/`, and `targets/` discovery model
- Added CLI commands for `init`, `inspect`, `metrics run`, and `serve`
- Added runner pipeline wiring for `metrics_job` decorator compatibility

0.0.1 - Initial release